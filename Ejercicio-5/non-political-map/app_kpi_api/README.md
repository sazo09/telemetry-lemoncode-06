## Start Locally

Install dependencies

```bash
uv sync
```

```bash
uv run fastapi dev
```

## Running Tests

```bash
# Run all tests
pytest -v
```

```bash
# Run and see the log output (to verify structured logs)
pytest -s
```
